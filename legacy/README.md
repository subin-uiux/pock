# Legacy (정적 마크업 참고)

React(Vite) 전환 이전의 루트 HTML / `css` / `js` / `data` 스냅샷입니다.

- 앱 진입점: 루트 `index.html` → `src/`
- 디자인 시스템: React `/guide` (`src/pages/GuidePage.tsx`)
- 옛 정적 가이드 원본: `legacy/pages/guide.html`
- 이 폴더는 실행하지 않습니다. 참고용으로만 둡니다.
